[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["**"]

[deploy]
# startCommand removed - using Dockerfile CMD instead (python /app/start.py)
# This prevents duplicate migration runs which cause hanging
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
